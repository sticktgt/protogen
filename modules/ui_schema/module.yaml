id: "ui_schema"
name: "UI Schema"
version: "0.1.0"

frontend:
  base_path: "modules/ui_schema"
  pages:
    - id: "main"
      title: "UI Schema"
      path: "index.html"
      menu:
        show: true
        title: "UI Schema"
        icon: "🧩"
        order: 30
      params:
        - "workspace_id"

backend:
  router: "backend.modules.ui_schema.api:router"
  api_prefix: "/api/ui-schema"

workspace:
  folder: "ui_schema"
  init:
    files: "modules/ui_schema/init"

docs:
  api: "modules/ui_schema/api.openapi.yaml"
  interop: "modules/ui_schema/interop.yaml"
