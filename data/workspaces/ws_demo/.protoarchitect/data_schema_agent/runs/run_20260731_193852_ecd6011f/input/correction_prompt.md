Исправь только перечисленные ошибки технической проверки временной схемы.

1. Один раз вызови `load_synchronization_context`.
2. Не выполняй новый полный дизайн и не меняй корректные части результата.
3. Используй patch-инструменты для существующих объектов и write-инструменты только для новых объектов.
4. Если ошибки затрагивают результаты требований, повторно запиши полный итог текущего пакета одним `write_data_schema_traceability`, используя точные канонические ID.
5. Последним отдельным вызовом запусти `validate_data_schema_state`.

Не добавляй предметные факты и значения справочников без прямого основания. Не повторяй отклонённый payload без исправления. Каждый ответ содержит ровно один tool call без промежуточного текста.