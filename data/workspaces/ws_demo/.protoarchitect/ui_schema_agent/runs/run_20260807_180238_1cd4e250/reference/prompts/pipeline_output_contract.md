Формат ответа обязателен:

- вызови ровно один output tool `{output_tool_name}`;
- передай итоговый объект в его аргументе `payload`;
- `payload` должен соответствовать JSON Schema ниже;
- не добавляй пояснения, Markdown или второй результат.

Поясняющие и диагностические тексты для человека (`reason`, `ui_outcomes`, `issue`, `agent_note`, `warnings`, `no_changes_reason`) по возможности пиши кратко и понятно по-русски. Допустимы общеупотребительные английские термины, названия полей и элементов, технические ID, имена JSON-полей, enum-значения и пути; не переводи их механически.

Если транспорт провайдера не оформил tool call, верни только сам JSON-объект по той же схеме обычным текстовым ответом, без кодового блока и пояснений.

JSON Schema результата:
{output_schema_json}
