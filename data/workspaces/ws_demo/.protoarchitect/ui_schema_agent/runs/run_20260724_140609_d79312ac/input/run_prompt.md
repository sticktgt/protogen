Выполни одну операцию синхронизации согласно `/input/task.json`.

Используй весь компактный файл `/input/requirements.agent.json`. В нём находятся все требования текущего запуска. Не дели пользовательскую задачу на независимые запуски.

Используй `/input/ui_schema_context.json` как полный снимок существующей UI-схемы. Не трать отдельные итерации на последовательное чтение каждого файла перед проектированием изменений.

Сначала составь план в `/scratch/plan.md`, затем запиши необходимые JSON-файлы инструментом `write_ui_schema_json`, подготовь итоговый отчёт и заверши работу инструментом `finish_ui_schema_sync`.